import RepoLink from '../RepoLink/RepoLink';

const Footer = () => {
  return (
    <footer className="flex h-8 items-center justify-center p-5">
      <RepoLink />
    </footer>
  );
};

export default Footer;
